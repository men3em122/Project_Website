# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/
build/

# Environment files (keep .env.example)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# TypeScript
*.tsbuildinfo

# Misc
.vercel
coverage/

# Local launcher scripts (not committed — local machine only)
start.ps1
stop.ps1

# Cloud credentials cache (never commit)
.env.main.secrets

# AI service — model weights are downloaded locally, never committed
ai-service/__pycache__/
ai-service/checkpoints/*
!ai-service/checkpoints/.gitkeep
ai-service/weights of yolo model/*
!ai-service/weights of yolo model/.gitkeep
ai-service/local_segformer/*
!ai-service/local_segformer/.gitkeep
