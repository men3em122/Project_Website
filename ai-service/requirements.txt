# Install inside the same Python environment where SAM2 is already installed
# (the env used by notebooks/interactive_sam.ipynb).
# SAM2 itself is installed from the local repo: pip install -e C:\Users\menna\Desktop\sam\sam2

fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
requests>=2.32
pydantic>=2.7

numpy<2
opencv-python
pillow
scikit-image
torch
transformers
ultralytics
